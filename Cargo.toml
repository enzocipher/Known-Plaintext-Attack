[package]
name = "Plaintext_XOR_Decryptor"
version = "0.1.0"
edition = "2024"
authors = ["Enzo Favio"]
description = "Simple algorithm to recover the key using a known plaintext."

[dependencies]
hex = "0.4"